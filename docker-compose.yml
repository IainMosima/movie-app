services:
  movie-app:
    build: .
    image: mosimaiain/movie-app:latest
    container_name: movie-app
    network_mode: host
    restart: unless-stopped
    volumes:
      - ${MEDIA_ROOT:-./data}:/app/data
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
      - PORT=8181
